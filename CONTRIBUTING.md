# Contributing to react-heatmap-chart

Thank you for your interest in contributing! This document explains how to set up the environment, the project conventions, and the expected workflow for submitting changes.

---

## Table of Contents

- [Prerequisites](#prerequisites)
- [Setting up the environment](#setting-up-the-environment)
- [Project structure](#project-structure)
- [Development workflow](#development-workflow)
- [Code conventions](#code-conventions)
- [Tests](#tests)
- [Opening a Pull Request](#opening-a-pull-request)
- [Reporting bugs](#reporting-bugs)
- [Suggesting improvements](#suggesting-improvements)

---

## Prerequisites

- **Node.js** >= 18
- **npm** >= 9
- Git

---

## Setting up the environment

1. **Fork** the repository and clone your fork:

```bash
git clone https://github.com/Bzutech/react-heatmap-chart
cd react-heatmap-chart
```

2. Install the dependencies:

```bash
npm install
```

3. Start watch mode to compile the library in real time:

```bash
npm run dev
```

The watch build uses `tsup` and automatically recompiles whenever a file is saved, generating the artifacts in `dist/`.

---

## Project structure

```
react-heatmap-chart/
├── src/
│   ├── index.ts                  # Public exports
│   ├── types.ts                  # All TypeScript types
│   ├── hooks.ts                  # Zoom, drag, draw hooks, etc.
│   ├── hooks.test.ts
│   ├── utils.ts                  # Pure canvas and geometry functions
│   ├── utils.test.ts
│   ├── heatmapImage.tsx          # HeatmapImage component
│   ├── canva.tsx                 # HeatmapCanvas (forwardRef)
│   ├── controls.tsx              # Zoom buttons
│   ├── controls.test.tsx
│   ├── emptyState.tsx            # Empty upload screen
│   ├── PlacingBanner.tsx         # Placement mode banner
│   ├── markers/
│   │   ├── markers.tsx           # Marker list (image mode)
│   │   ├── marker.item.tsx       # Individual marker with hover/tooltip
│   │   └── marker.pin.tsx        # Pin visual (circle + icon)
│   ├── tooltip/
│   │   ├── Tooltip.tsx           # Tooltip with adaptive positioning
│   │   ├── tooltip.hook.ts       # usePlacement
│   │   ├── tooltip.hook.test.ts
│   │   ├── tooltip.styles.ts     # Tooltip styles by placement
│   │   └── tooltip.styles.test.ts
│   └── map/
│       ├── map.tsx               # HeatmapMap (main OSM component)
│       ├── map.render.tsx        # HeatmapMapDynamic (lazy wrapper)
│       ├── map.layer.tsx         # HeatmapMapLayer (canvas over Leaflet)
│       ├── map.marker.tsx        # MapMarkers (react-leaflet)
│       ├── map.marker.icon.ts    # buildMapIcon (divIcon)
│       ├── map.tooltip.tsx       # TooltipContent (OSM)
│       ├── map.controller.ts     # MapViewController (flyTo/fitBounds)
│       ├── map.clickHandler.ts   # MapClickHandler
│       ├── map.config.ts         # DEFAULT_OSM_CONFIG + useMapConfig hook
│       ├── map.config.test.ts
│       ├── map.utils.ts          # drawHeatmapOSM + metersToPixels
│       └── map.utils.test.ts
└── dist/                         # Generated by the build (do not version)
```

---

## Development workflow

### 1. Create a branch from `dev`

Use descriptive names following the `<type>/<short-description>` pattern:

```bash
git checkout -b feat/organic-polygon-mode
git checkout -b fix/tooltip-overflow-on-edge
git checkout -b docs/contributing-guide
git checkout -b refactor/use-draw-cleanup
```

| Prefix | When to use |
|--------|-------------|
| `feat/` | New feature |
| `fix/` | Bug fix |
| `docs/` | Documentation only |
| `refactor/` | Refactoring without behavior changes |
| `chore/` | Configuration, dependencies, CI |
| `test/` | Adding or fixing tests |

### 2. Develop and compile

```bash
npm run dev   # watch mode
```

Test your changes by linking the library in a local project:

```bash
# At the root of react-heatmap-chart
npm run build
npm link

# In the test project
npm link react-heatmap-chart
```

### 3. Make semantic commits

```
feat: add angleSweep support for arc sectors
fix: clamp pan on container resize
docs: add tooltip customisation example
refactor: extract gradient logic into addThermalStops
test: add unit tests for clampPan utility
```

Format: `<type>: <imperative description, in English, without a period>`

### 4. Open the Pull Request

Follow the instructions in the [Opening a Pull Request](#opening-a-pull-request) section.

---

## Code conventions

### TypeScript

- All files are `.ts` or `.tsx`. No plain JavaScript.
- Prop types should live in `src/types.ts`. Avoid declaring types locally if they can be reused.
- Prefer `interface` for component props and `type` for unions and aliases.

### React components

- Always use functional components. No class components.
- Custom hooks should live in `hooks.ts` (image mode) or close to the file that uses them (map mode).
- Styles should be typed `CSSProperties` objects using `satisfies` — no external CSS and no runtime CSS-in-JS.

```tsx
// ✅ correct
const S = {
  root: {
    position: "relative",
    width: "100%",
  } satisfies React.CSSProperties,
} as const;

// ❌ avoid
const styles = { root: { position: "relative" } };
```

### Canvas / utilities

- Canvas functions should live in `utils.ts` (image mode) or `map/map.utils.ts` (OSM mode).
- Functions should be pure whenever possible — they should receive the canvas and data, and avoid accessing external state.
- Prefer using `clamp` from `utils.ts` instead of inline `Math.min(Math.max(...))`.

### Exports

- Only export from `src/index.ts` what is part of the public API.
- Internal types (for example, `ImageRect`, `Pan`) should only be exported when needed by the consumer.

---

## Tests

Tests are colocated with the files they cover — place the test file in the same folder as the source file.

### Where to place tests

```
src/
├── utils.test.ts
├── hooks.test.ts
├── controls.test.tsx
├── map/
│   ├── map.utils.test.ts
│   └── map.config.test.ts
└── tooltip/
    ├── tooltip.hook.test.ts
    └── tooltip.styles.test.ts
```

### Running tests

```bash
npm run test:run      # single run (CI)
npm run test:watch    # watch mode (development)
npm run test:coverage # coverage report
```

### What to test

1. **Pure utilities** — `clamp`, `clampPan`, `getContainRect`, `metersToPixels`, `toScreenCoords`, `sampleThermal`
2. **Hooks** — `useZoom`, `usePlaceClick`, `useImageRect`, `usePlacement`, `useMapConfig`
3. **Components** — `MarkerPin`, `Tooltip`, `HeatmapControls`

### Best practices

- Each pure function should have at least one test for the happy path and one for edge values (0, 100, NaN).
- Do not test implementation details — test observable behavior.
- `jsdom` does not implement `canvas.getContext()` — mock it via `vi.spyOn(canvas, "getContext")` before calling any canvas function.
- `ResizeObserver` mocks may be required for hooks that observe container size.

---

## Opening a Pull Request

1. Make sure the build and tests pass:

```bash
npm run build
npm run test:run
```

2. Push your branch to your fork:

```bash
git push origin feat/my-feature
```

3. Open a PR in the original repository targeting **`dev`**.

4. Fill out the PR template with:
   - **What changes** — objective description of what was done
   - **Why** — motivation or related issue
   - **How to test** — steps to reproduce or verify
   - **Screenshots** (if there are visual changes)

5. Wait for review. Changes may be requested — this is normal and part of the process.

### Checklist before opening the PR

- [ ] `npm run build` runs without errors
- [ ] `npm run test:run` passes without errors
- [ ] New or modified code has corresponding tests
- [ ] No new `any` was introduced without justification
- [ ] Added or modified types are in `src/types.ts`
- [ ] New public exports are in `src/index.ts`
- [ ] Documentation was updated if the API changed (`README.md` or `docs/`)

---

## Reporting bugs

Open an [issue](https://github.com/Bzutech/react-heatmap-chart/issues/new) with:

- **Library version** (`npm list react-heatmap-chart`)
- **Description of expected vs. observed behavior**
- **Minimal reproducible code** (snippet or sandbox)
- **Environment** — browser, Node version, bundler

---

## Suggesting improvements

Open an issue with the `enhancement` label describing:

- The problem the improvement would solve
- The solution you have in mind
- Alternatives you considered

If you want to implement it yourself, comment on the issue before opening a PR to avoid duplicated work.

---

## Questions

Open an issue with the `question` label or contact the repository maintainers. Contributions of any level are welcome.